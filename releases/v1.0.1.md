# v1.0.1

- Each visual effect the editor lists and lets the user move is now documented as its own document.
- Adds a default prompt reference for the `aip` skill.
- Points the plugin homepage at the product main page instead of the docs site.

**Full changelog**: https://github.com/opus-pro/ai-producer-plugin/compare/v1.0.0...v1.0.1
